
let name = "okoye chidiebube";  // Assigning a string value to the variable 'name'

// Using string concatenation to create a new string
let greeting = "Hello, " + name + "!";

// Using string methods to manipulate the string
let upperCase = name.toUpperCase();  // Converting the string to uppercase
let lowerCase = name.toLowerCase();  // Converting the string to lowercase
let slicedString = name.slice(0, 5);  // Slicing the string to extract a portion

// Printing the results
console.log(greeting);
console.log(upperCase);
console.log(lowerCase);
console.log(slicedString);

